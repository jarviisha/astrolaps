---
// Preload critical images
const imageUrl = "/drawp.png";
---

<section class="w-full mt-40" id="projects">
  <div class="container mt-12 justify-center mx-auto h-full text-white">
    <div>
      <h1 class="font-header text-9xl text-center">LATEST-PROJECT</h1>
    </div>
    
    <!-- Optimized grid layout with better responsive design -->
    <div class="grid grid-cols-12 gap-2 w-full mt-24" role="region" aria-label="Latest projects">
      <div
        class="text-4xl col-span-12 md:col-span-8 h-96 bg-[#c98a8c]/60 flex items-center justify-center transition-colors duration-300 hover:bg-[#c98a8c]/80"
        role="article"
        aria-label="Project placeholder 1"
      >
        <span class="sr-only">Project coming soon - </span>
        <span aria-hidden="true">coming soon</span>
      </div>
      <div
        class="text-4xl col-span-12 md:col-span-4 h-96 bg-[#dfcb8f]/60 flex items-center justify-center transition-colors duration-300 hover:bg-[#dfcb8f]/80"
        role="article"
        aria-label="Project placeholder 2"
      >
        <span class="sr-only">Project coming soon - </span>
        <span aria-hidden="true">coming soon</span>
      </div>
      <div
        class="text-4xl col-span-12 md:col-span-6 h-96 bg-[#c8d0d2]/60 flex items-center justify-center transition-colors duration-300 hover:bg-[#c8d0d2]/80"
        role="article"
        aria-label="Project placeholder 3"
      >
        <span class="sr-only">Project coming soon - </span>
        <span aria-hidden="true">coming soon</span>
      </div>
      <div
        class="text-4xl col-span-12 md:col-span-6 h-96 bg-[#f9d2bd]/60 flex items-center justify-center transition-colors duration-300 hover:bg-[#f9d2bd]/80"
        role="article"
        aria-label="Project placeholder 4"
      >
        <span class="sr-only">Project coming soon - </span>
        <span aria-hidden="true">coming soon</span>
      </div>
    </div>
    
    <!-- Optimized quote section with lazy loading image -->
    <div class="w-full relative mt-40 text-white">
      <p class="text-4xl md:text-6xl lg:text-8xl font-script z-10 leading-tight">
        The beauty of code lies not in flashy syntax or elaborate architecture,
        but in the eyes of a passionate developer – the one who sees elegance
        hidden within the logic.
      </p>
      <div class="absolute w-[300px] h-[250px] md:w-[600px] md:h-[500px] top-[-50px] md:top-[-100px] right-[-210px] md:right-[-420px] -z-10">
        <img 
          class="w-full h-auto" 
          src={imageUrl}
          alt="Decorative illustration"
          loading="lazy"
          decoding="async"
          width="600"
          height="500"
        />
      </div>
    </div>
  </div>
</section>

<style>
  /* Optimize font loading */
  .font-header {
    font-display: swap;
  }
  
  .font-script {
    font-display: swap;
  }
  
  /* Optimize animations */
  @media (prefers-reduced-motion: reduce) {
    .transition-colors {
      transition: none;
    }
  }
  
  /* Optimize for mobile performance */
  @media (max-width: 768px) {
    .text-9xl {
      font-size: 4rem;
    }
  }
</style>

<!-- Preload critical resources -->
<link rel="preload" href={imageUrl} as="image" />
